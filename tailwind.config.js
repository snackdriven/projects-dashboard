/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Tailwind v4: Theme configuration is now done in CSS using @theme directive
  // This config file is optional and primarily for IDE support
}

